# Nginx配置片段 - 用于处理storage目录
# 将此配置添加到你的nginx站点配置中

location /storage/ {
    # 不直接访问文件，而是转发给PHP处理
    try_files $uri @rewrite;
}

location @rewrite {
    rewrite ^/(.*)$ /index.php?s=/$1 last;
}

# 或者更简单的方式：
location /storage/ {
    rewrite ^/(.*)$ /index.php last;
}
